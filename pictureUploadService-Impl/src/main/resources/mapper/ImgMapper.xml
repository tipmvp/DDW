<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ddw.pictureUploadServiceImpl.dao.PictureDao" >
   	<!--<select id="getPicture" resultType="Picture" parameterType="int">-->
   	 <!--select * from img where id = #{id}-->
   	<!--</select>-->
    <insert id="insertPicture" parameterType="Picture">
   	 insert into img(picName,url) values(#{picName},#{url})
   	</insert>
    <!--<select id="getAllPictures" resultType="Picture">-->
   	 <!--select * from img-->
   	<!--</select>-->
</mapper>
